gsap.registerPlugin(Flip);

let link_text = document.querySelector(".gik-link").textContent;

document.addEventListener("DOMContentLoaded", function() {
    document.querySelector(".gik-link").addEventListener(
        "mouseover",
        () => {
        }
    );
});     
